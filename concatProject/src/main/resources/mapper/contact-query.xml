<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="deletecontact">
        DELETE FROM tbl_contact
        WHERE phonenumber = ?
    </entry>

    <entry key="insertcontact">
        INSERT INTO tbl_contact(
        contact_name,
        phonenumber,
        email,
        address,
        birthday,
        groupnumber,
        usercode
        )VALUES(
        ?,
        ?,
        ?,
        ?,
        ?,
        ?,
        ?
        )
    </entry>

    <entry key="updateMenu">
        UPDATE tbl_contact
        SET contact_name = ? , phonenumber = ? , email = ?, address = ?, birthday = ?, groupnumber = ?, usercode = ?
        WHERE phonenumber = ?
    </entry>


    <entry key = "findPhoneNumber">

        SELECT
        contact_code,
        contact_name,
        phonenumber,
        birthday,
        G.groupname
        FROM tbl_contact C
        JOIN tbl_group G ON C.groupnumber = G.groupnumber;


    </entry>

    <entry key="countAllContact">
        SELECT count(*)
        FROM tbl_contact
        where user_code =?
    </entry>

    <entry key="groupCount">
        SELECT groupname,count(*)
        From tbl_contact a JOIN tbl_group b
        on a.groupnumber = b.groupnumber
        where user_code =?
        group by groupname
    </entry>

    <entry key="selectLastContact">
        SELECT contact_code,contact_name,phonenumber,email,adress,birthday,groupnumber,user_code
        FROM tbl_contact
        where user_code =?
        order by contact_code DESC
        Limit 1
    </entry>


    <entry key = "findGroup">
        SELECT
        contact_code,
        contact_name,
        phonenumber,
        birthday,
        G.groupname
        FROM tbl_contact C
        JOIN tbl_group G ON C.groupnumber = G.groupnumber
        WHERE G.groupname = ?


    </entry>


</properties>